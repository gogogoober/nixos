Scratch space, gitignored. Not part of the system config.

- `references/` — cloned or copied example configs we are learning from
- `prds/` — distilled product requirement docs describing the specific changes we want before we touch the real config

Workflow: read references, write a PRD in `prds/`, agree on it, then apply the changes to the actual Nix modules.
